name: LLL

on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Get Code
        uses: actions/checkout@v3
        with:
          path: project1
      - name: Install NodeJS
        uses: actions/setup-node@v6
        with:
          
